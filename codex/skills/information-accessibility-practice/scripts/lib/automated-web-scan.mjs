import crypto from "node:crypto";
import fs from "node:fs";
import path from "node:path";
import { createRequire } from "node:module";
import { fileURLToPath } from "node:url";
import { collectWebEvidence, settlePage, withWebInspectionSession } from "../capture-web-evidence.mjs";
import { validateJsonSchema } from "./json-schema.mjs";

const MAX_TEXT_CODE_POINTS = 2000;
const MAX_CONTEXT_ITEMS = 100;
const MAX_CONTEXT_NODES = 20;
const MAX_CONTEXT_BYTES = 512 * 1024;
const SUPPORTED_AXE = "4.13.0";
const SUPPORTED_PLAYWRIGHT = "1.62.1";
const scriptRoot = path.dirname(fileURLToPath(import.meta.url));
const skillRoot = path.resolve(scriptRoot, "../..");
const require = createRequire(import.meta.url);
function sha256(value) { return crypto.createHash("sha256").update(value).digest("hex"); }
function canonical(value) { if (Array.isArray(value)) return value.map(canonical); if (value && typeof value === "object") return Object.fromEntries(Object.keys(value).sort().map((key) => [key, canonical(value[key])])); return value; }
function canonicalJson(value) { return JSON.stringify(canonical(value)); }
function readJson(file) { return JSON.parse(fs.readFileSync(file, "utf8").replace(/^\uFEFF/u, "")); }
export function truncateCodePoints(value, limit = MAX_TEXT_CODE_POINTS) { const points = Array.from(String(value ?? "")); return { value: points.slice(0, limit).join(""), truncated: points.length > limit }; }
export function successCriterionFromAxeTag(tag) { const match = /^wcag([1-4])([1-9])([0-9]+)$/u.exec(String(tag)); return match ? `${match[1]}.${match[2]}.${Number(match[3])}` : null; }
export function normalizeOrigin(value) { const url = new URL(value); if (!["http:", "https:"].includes(url.protocol)) throw new Error("Origin must use http or https."); if (url.username || url.password) throw new Error("Origin must not contain credentials."); if (url.search || url.hash || (url.pathname && url.pathname !== "/")) throw new Error("Origin must not contain path, query, or fragment data."); if (url.hostname.endsWith(".")) throw new Error("Trailing-dot hostnames are not accepted."); if (url.hostname.includes("*")) throw new Error("Wildcard origins are not accepted."); return url.origin; }
export function profileRequirementMap(profileId, standardsRegistry, criteriaCatalog) { const profile = standardsRegistry.profiles?.find((entry) => entry.id === profileId); if (!profile || profile.implementation_status !== "active" || profile.assessment_configuration?.active !== true) throw new Error(`Profile is unknown or inactive: ${profileId}`); const result = new Map(); for (const catalogKey of profile.assessment_configuration.catalog_keys ?? []) for (const item of criteriaCatalog.catalogs?.[catalogKey] ?? []) { const values = result.get(item.success_criterion) ?? []; values.push(item.id); result.set(item.success_criterion, [...new Set(values)].sort((a, b) => a.localeCompare(b, "en"))); } return result; }
function criterionIds(tags, profileMap) { const ids = new Set(); for (const tag of tags ?? []) { const criterion = successCriterionFromAxeTag(tag); for (const id of criterion ? (profileMap.get(criterion) ?? []) : []) ids.add(id); } return [...ids].sort((a, b) => a.localeCompare(b, "en")); }
function identitySelector(selector) { return String(selector).replace(/:nth-(?:child|of-type)\(\s*\d+\s*\)/giu, ":nth-(*)").replace(/\s+/gu, " ").trim(); }
function normalizeNode(node) { const html = truncateCodePoints(node.html); const failure = truncateCodePoints(node.failureSummary); const targets = (node.target ?? []).map(String).sort((a, b) => a.localeCompare(b, "en")); return { targets, identity_targets: targets.map(identitySelector), html: html.value, html_truncated: html.truncated, failure_summary: failure.value, failure_summary_truncated: failure.truncated }; }
function sanitizeUrl(value) { try { const url = new URL(value); url.username = ""; url.password = ""; url.search = ""; url.hash = ""; return url.href; } catch { return "withheld-invalid-url"; } }
function normalizeRule(rule, kind, source, frame, engine, profileMap) { const profileIds = criterionIds(rule.tags, profileMap); const nodes = (rule.nodes ?? []).map(normalizeNode); const groups = new Map(); for (const node of nodes.length ? nodes : [{ targets: [], identity_targets: [], html: "", html_truncated: false, failure_summary: "", failure_summary_truncated: false }]) { const identity = canonicalJson({ engine, rule_id: rule.id, frame_path: frame.path, frame_url: sanitizeUrl(frame.url), targets: node.identity_targets, failure_summary: node.failure_summary, impact: rule.impact ?? null }); const key = sha256(identity); const existing = groups.get(key) ?? []; existing.push(node); groups.set(key, existing); } return [...groups.entries()].map(([dedupKey, groupedNodes]) => ({ dedup_key: dedupKey, kind, source, rule_id: rule.id, impact: rule.impact ?? null, help: String(rule.help ?? ""), help_url: rule.helpUrl ?? null, tags: [...new Set(rule.tags ?? [])].sort((a, b) => a.localeCompare(b, "en")), criterion_relation: "reference_only", profile_requirement_ids: profileIds, occurrence_count: groupedNodes.length, frame: { path: frame.path, url: sanitizeUrl(frame.url) }, nodes: groupedNodes.map(({ identity_targets, ...node }) => node) })); }
function ruleSummary(rule) { return { rule_id: rule.id, impact: rule.impact ?? null, help: String(rule.help ?? ""), help_url: rule.helpUrl ?? null, tags: [...new Set(rule.tags ?? [])].sort((a, b) => a.localeCompare(b, "en")) }; }
function ruleOrder(a, b) { return String(a.rule_id).localeCompare(String(b.rule_id), "en"); }
function sortItems(items) { return [...items].sort((a, b) => a.rule_id.localeCompare(b.rule_id, "en") || a.dedup_key.localeCompare(b.dedup_key, "en")); }
export function normalizeAxeResults({ axeResults, profileMap, frame, engine }) { const machineViolations = []; const reviewCandidates = []; const unmappedFindings = []; for (const rule of axeResults.violations ?? []) for (const item of normalizeRule(rule, "machine_violation", "axe-core", frame, engine, profileMap)) { if (item.profile_requirement_ids.length > 0) machineViolations.push(item); else unmappedFindings.push({ ...item, kind: "unmapped_finding" }); } for (const rule of axeResults.incomplete ?? []) reviewCandidates.push(...normalizeRule(rule, "review_candidate", "axe-core", frame, engine, profileMap)); return { machine_violations: sortItems(machineViolations), review_candidates: sortItems(reviewCandidates), unmapped_findings: sortItems(unmappedFindings), machine_passes: (axeResults.passes ?? []).map(ruleSummary).sort(ruleOrder), inapplicable: (axeResults.inapplicable ?? []).map(ruleSummary).sort(ruleOrder) }; }
function compactItem(item) { return { ...item, nodes: (item.nodes ?? []).slice(0, MAX_CONTEXT_NODES), nodes_truncated: (item.nodes?.length ?? 0) > MAX_CONTEXT_NODES }; }
export function buildAutomatedScanContext(scan, sourceScanSha256) { const violations = (scan.machine_violations ?? []).map(compactItem); const candidates = (scan.review_candidates ?? []).map(compactItem); const prioritized = [...violations, ...candidates]; const items = prioritized.slice(0, MAX_CONTEXT_ITEMS); const omittedByClass = { machine_violations: Math.max(0, violations.length - items.filter((item) => item.kind === "machine_violation").length), review_candidates: Math.max(0, candidates.length - items.filter((item) => item.kind === "review_candidate").length) }; const context = { schema_version: "1.0.0", kind: "automated-web-scan-context", stability: "experimental", source_scan_sha256: sourceScanSha256, target: { requested_url: sanitizeUrl(scan.target.requested_url), final_url: sanitizeUrl(scan.target.final_url), http_status: scan.target.http_status, dom_sha256: scan.target.dom_sha256, ax_tree_sha256: scan.target.ax_tree_sha256 }, environment: scan.environment, frame_coverage: { attempted: scan.frame_coverage.attempted, succeeded: scan.frame_coverage.succeeded, failed: scan.frame_coverage.failed, skipped: scan.frame_coverage.skipped }, summary: scan.summary, items, focus_summary: { steps: scan.evidence?.focus_path?.length ?? 0, active_element: scan.evidence?.active_element ?? null }, reflow_summary: scan.evidence?.reflow ?? null, truncation: { truncated: prioritized.length > items.length, omitted_items: prioritized.length - items.length, omitted_nodes: items.reduce((total, item) => total + (item.nodes_truncated ? Math.max(0, (prioritized.find((source) => source.dedup_key === item.dedup_key)?.nodes?.length ?? 0) - MAX_CONTEXT_NODES) : 0), 0), omitted_by_class: omittedByClass, reason: prioritized.length > items.length ? "context_limit" : "none" } }; while (Buffer.byteLength(JSON.stringify(context), "utf8") > MAX_CONTEXT_BYTES && context.items.length > 0) { const removed = context.items.pop(); context.truncation.truncated = true; context.truncation.omitted_items += 1; context.truncation.omitted_by_class[removed.kind === "machine_violation" ? "machine_violations" : "review_candidates"] += 1; context.truncation.reason = "context_limit"; } if (Buffer.byteLength(JSON.stringify(context), "utf8") > MAX_CONTEXT_BYTES) throw new Error("Compact scan context exceeds the 512 KiB limit even without item details."); return context; }
export function scanSha256(scan) { return sha256(canonicalJson(scan)); }

function packageVersion(name) { const manifest = readJson(require.resolve(`${name}/package.json`)); return manifest.version; }
async function loadAxe() { let imported; try { imported = await import("axe-core"); } catch { throw new Error(`axe-core is not installed. Install axe-core@${SUPPORTED_AXE} and retry.`); } const axe = imported.default ?? imported; const version = packageVersion("axe-core"); if (version !== SUPPORTED_AXE) throw new Error(`Unsupported axe-core version ${version}; expected ${SUPPORTED_AXE}.`); return { axe, version }; }
function assertPlaywrightVersion() { let version; try { version = packageVersion("playwright"); } catch { throw new Error(`Playwright is not installed. Install playwright@${SUPPORTED_PLAYWRIGHT} and Chromium, then retry.`); } if (version !== SUPPORTED_PLAYWRIGHT) throw new Error(`Unsupported Playwright version ${version}; expected ${SUPPORTED_PLAYWRIGHT}.`); return version; }
function framePath(frame, frames) { const index = frames.indexOf(frame); const chain = []; let current = frame; while (current) { chain.unshift(frames.indexOf(current)); current = current.parentFrame(); } return chain.join(".") || String(index); }
async function runAxeInFrame(frame, axeSource) { return frame.evaluate(async (source) => { Function(source)(); return globalThis.axe.run(document, { resultTypes: ["violations", "incomplete", "passes", "inapplicable"] }); }, axeSource); }
function mergeNormalized(target, normalized) { target.machine_violations.push(...normalized.machine_violations); target.review_candidates.push(...normalized.review_candidates); target.unmapped_findings.push(...normalized.unmapped_findings); target.machine_passes.push(...normalized.machine_passes); target.inapplicable.push(...normalized.inapplicable); }
async function reflowProbe(page, width, primaryViewport, profileMap) { await page.setViewportSize({ width, height: primaryViewport.height }); await settlePage(page); const measured = await page.evaluate(() => { const root = document.documentElement; const candidates = []; for (const el of document.querySelectorAll("body *")) { const style = getComputedStyle(el); if (style.display === "none" || style.visibility === "hidden") continue; const rect = el.getBoundingClientRect(); if (rect.width <= 0 || rect.height <= 0 || rect.right <= innerWidth + 1) continue; let scrollContainer = false; for (let parent = el.parentElement; parent; parent = parent.parentElement) { const parentStyle = getComputedStyle(parent); if (["auto", "scroll"].includes(parentStyle.overflowX) && parent.scrollWidth > parent.clientWidth) { scrollContainer = true; break; } } if (!scrollContainer) candidates.push({ selector: el.id ? `#${CSS.escape(el.id)}` : el.tagName.toLowerCase(), right: Math.round(rect.right), width: Math.round(rect.width) }); if (candidates.length >= 50) break; } return { document_scroll_width: root.scrollWidth, document_client_width: root.clientWidth, candidates }; }); const capturedAt = new Date().toISOString(); await page.setViewportSize(primaryViewport); await settlePage(page); const requirementIds = profileMap.get("1.4.10") ?? []; return { captured_at: capturedAt, viewport: { width, height: primaryViewport.height }, ...measured, profile_requirement_ids: requirementIds };
}
function validateAgainstSchema(value, schemaFile, label) { const schema = readJson(schemaFile); const errors = []; validateJsonSchema(value, schema, "$", errors); if (errors.length) throw new Error(`${label} failed schema validation:\n- ${errors.join("\n- ")}`); }

export async function runAutomatedWebScan(options) {
  assertPlaywrightVersion();
  const { axe, version: axeVersion } = await loadAxe();
  const registry = readJson(path.join(skillRoot, "references", "standards-registry.json"));
  const catalog = readJson(path.join(skillRoot, "references", "criteria-catalog.json"));
  const profileMap = profileRequirementMap(options.profile, registry, catalog);
  return withWebInspectionSession({ ...options, allowedMethods: new Set(["GET", "HEAD"]) }, async (session) => {
    const frames = session.page.frames();
    const aggregate = { machine_violations: [], review_candidates: [], unmapped_findings: [], machine_passes: [], inapplicable: [] };
    const entries = [];
    const raw = [];
    for (const frame of frames) {
      const descriptor = { url: frame.url(), path: framePath(frame, frames) };
      try {
        const result = await runAxeInFrame(frame, axe.source);
        raw.push({ frame: descriptor, result });
        mergeNormalized(aggregate, normalizeAxeResults({ axeResults: result, profileMap, frame: descriptor, engine: { name: "axe-core", version: axeVersion } }));
        entries.push({ frame_path: descriptor.path, url: sanitizeUrl(descriptor.url), status: "succeeded", reason: null });
      } catch (error) {
        const reason = truncateCodePoints(error instanceof Error ? error.message : String(error), 500).value;
        entries.push({ frame_path: descriptor.path, url: sanitizeUrl(descriptor.url), status: "failed", reason });
        aggregate.review_candidates.push({ dedup_key: sha256(`frame-error:${descriptor.path}:${sanitizeUrl(descriptor.url)}:${reason}`), kind: "review_candidate", source: "axe-frame-error", rule_id: "frame-scan", impact: null, help: "Frame could not be scanned automatically", help_url: null, tags: [], criterion_relation: "reference_only", profile_requirement_ids: [], occurrence_count: 1, frame: { path: descriptor.path, url: sanitizeUrl(descriptor.url) }, nodes: [] });
      }
    }
    const evidenceBundle = await collectWebEvidence(session, options);
    const reflow = await reflowProbe(session.page, options.reflowWidth ?? 320, options.viewport ?? { width: 1280, height: 800 }, profileMap);
    if (reflow.candidates.length) aggregate.review_candidates.push({ dedup_key: sha256(canonicalJson({ rule: "reflow-overflow", candidates: reflow.candidates })), kind: "review_candidate", source: "internal-reflow-probe", rule_id: "reflow-overflow", impact: null, help: "Horizontal overflow detected at the 320 CSS-pixel proxy viewport", help_url: null, tags: ["wcag1410"], criterion_relation: "reference_only