# Issueの優先順位と実装範囲

2026-09-19時点で、未完了Issueは37件（整理用の#144を含む）、未完了PRは0件。実装の有無とIssueの完了は区別し、受け入れ条件が残るものは未完了として扱う。

最初に、人が確認した不適合を保存・報告する経路を整える。次に、その証拠から許される主張範囲を選択可能にし、中断した監査の再開に必要な状態表示を加える。

## 最初の実装単位

| 順序 | Issue | 完了条件 |
| --- | --- | --- |
| 1 | [#56](https://github.com/Nagi-Inaba/information-accessibility-skill/issues/56) 改善計画前の不適合記録 | 人手レビューの指摘を改善計画なしで統合・報告でき、後から計画を追加できる。証拠・箇所・影響・優先度が欠ける指摘は拒否する。 |
| 2 | [#40](https://github.com/Nagi-Inaba/information-accessibility-skill/issues/40) 人手評価と主張範囲 | 登録済み証拠を共通guardで評価し、固定文言でreference_only／screened／evaluated_subsetを選べる。AIによる昇格と証拠にない書換えを拒否する。 |
| 3 | [#44](https://github.com/Nagi-Inaba/information-accessibility-skill/issues/44) 状態・次の操作 | 読取り専用CLIで状態、成果物、検証結果、次の遷移、統合・報告・再検査の可否、評価範囲、後続runの存在を表示する。 |

各Issueの受け入れ条件を検証して進捗を記録し、後続の優先順位に従って継続する。回帰テスト、Codex／Claude配布物の同期、パッケージ検証を行う。push、PR作成、Issueのcloseは統合とは別の操作であり、ローカル実装だけでは完了済みに変更しない。

## 2026-09-18のローカル実装

作業branchは `fix/prioritized-audit-workflow`。最初の3件はローカル実装済みで、まだpush・PR作成・mergeしていない。GitHub上のIssueはopenのまま維持する。

- **#56**: 人手レビューに指摘の必須情報を保存し、改善計画がなくても統合・報告できる。未策定は `remediation_status: unplanned` と表示し、後から計画・担当者・検証方法を追加しても指摘を保持する。
- **#40**: `merge --claim-tier` で `reference_only`／`screened`／`evaluated_subset` を選べる。共通guardと固定文言で証拠の範囲を検証し、人手評価者の本人確認がないという制約も表示する。
- **#44**: `status --run ... --format text|json --locale ja|en` で状態、成果物、評価範囲、権限、次の遷移、操作の可否を確認できる。同じディレクトリ内の後続run・分岐を警告する。別ディレクトリのコピーは検索しない。
- **#27の一部**: Windows／Node 22.19.0で確認したClaude導入時のコピー処理のクラッシュを修正した。更新・削除など、Issue全体の受け入れ条件は残る。

主実装は `4043527`、Claude導入修正とスキル説明の整合は `35d9dc5`、不正なrunの状態表示とREADME日英リンクの修正は `1dde323` に保存済み。過去runのresource hashは書き換えず、移行は#28の残課題として扱う。

### 検証結果

- 実装関連の119テストが成功。状態表示の追加修正後も関連4テストが成功した。
- 全57テストファイルを実行し、494件中485成功・1失敗・8スキップ。失敗したREADMEの日英リンク差分を修正し、該当ファイルの6テストすべてが再検証で成功した。未解消のテスト失敗はない。
- Codex／Claudeの配布物同期、パッケージ検証、criteria catalog検証、`git diff --check` が成功した。
- ブラウザ等の環境依存テスト8件はスキップ。実ブラウザ・支援技術での動作確認や、Ubuntu／WindowsのリモートCIは今回の成功に含めない。
- Windows PowerShellの導入テストでは、実行プロセス内の `PSModulePath` をWindows PowerShell標準モジュールの場所に限定した。常設の環境設定は変更していない。

## 後続の優先順位

### 継続実装の進捗

| Issue | 状態 | 実装・検証する範囲 |
| --- | --- | --- |
| #39 | ローカル実装・検証済み | RFC 3339のoffsetを保持し、履歴・入力順・fixerの期限を実時刻と小数秒で比較。実在日付・うるう年・DST・旧UTC形式のテストが成功。全57テストファイルの検証成功。 |
| #25 | ローカル実装・検証済み | screening payload 3.0.0でE1に保存済み証拠を必須化。run・宣言対象・環境の照合、登録後のhash再検証、公開レポートからの非公開参照の除外、証拠追加・前後比較CLIを実装。専用9テストと全58テストファイルの検証が成功。 |
| #54 | ローカル実装・検証済み（2026-09-19） | run 8／registry 7／envelope 3で実測inventoryとsnapshot IDを固定。登録時の再測定、DOM・AX照合、修正後の再検査、非公開の前後比較を接続。追加9テスト成功、関連137件中136成功・環境依存1スキップ、全62テストファイルの検証成功。 |
| #65 | ローカル実装・検証済み（2026-09-19） | axe-core 4系のversioned importerを追加。tool・設定・raw hash・対象snapshotを保持し、未知rule、重複キー、互換性、公開範囲を検証。実Chromeでscanner→import→register→reportが成功し、全64テストファイルの検証成功。リモートCIは未実行。 |
| #13 | ローカル実装・検証済み（2026-09-19） | 6能力の機械可読契約とpreflight CLIを追加。対象DNS解決前に固定fixtureでDOM・AX・Tab・viewport・通信制御を実測し、不足能力と次のテストを表示。実ChromeのE2Eと全66テストファイルの一括検証成功。 |
| #63 | ローカル実装・検証済み（2026-09-19） | run 9／registry 8で対象・規格資料の具体的な許可先を保存。呼出側との積集合、転送ごとのIP固定、GET／HEAD・資源・容量制限、非公開通信ログの証拠への紐付け、status／reportを接続。専用16テストと全70テストファイル一括実行が成功。 |
| #64 | ローカル実装・検証済み（2026-09-19） | run 10／registry 9で監督者・具体的操作・範囲・期限と承認単位を必須化。実行時の承認、追記ログ、失効・取消時のread_only復帰を接続。専用11テストと全72テストファイル一括実行が成功。対応操作はスクリプト・追加通信を停止したページのTab／Shift+Tab。 |
| #57 | ローカル実装・検証済み（2026-09-19） | assessment 2.0.0で人手申告行と元レビューを結合。単独apply／run付きmerge、外部方針による署名者確認、claim guard、日英の公開Markdown／HTML、旧形式表示を接続。統合6テストと既存CLI拡張が成功。全76ファイルの実行と失敗分の再検証が完了。 |
| #58 | ローカル実装・検証済み（2026-09-19） | bundle記録1.0.0とprepare／verify CLIを追加。run・登録artifact・証拠・assessment・report・明示した添付を署名対象とし、現在の実バイトと前段の署名をオフライン検証。専用7テスト成功。全77ファイルの実行と失敗分の修正・再検証が完了。 |
| #50 | ローカル実装・検証済み（2026-09-19） | 出典・利用条件の台帳、同梱notice、カタログ更新候補の再確認待ち記録、日英Markdown／HTMLへの帰属表示を追加。専用7テスト、全78ファイル、実Chromeのレポート検査が成功。法的レビューは未実施と明記。 |
| #35 | ローカル実装・検証済み／外部反映待ち（2026-09-19） | 対応版・移行方針、4種類のIssueフォーム、非公開報告の案内、commit固定の配布候補と収録一覧を整備。全79ファイル623件中618成功・失敗0・5スキップ。GitHub設定は確認回答待ち、Release公開は未実施。 |
| #16 | ローカル実装・検証済み（2026-09-19） | run 11／registry 10／queue 3で対象箇所・登録済み観測・理由・優先度を結合。候補生成CLI、公開レポート、旧形式読取り、重複検出を接続。全80ファイルの実行と失敗分の修正・関連120件の再検証が完了。 |
| #17 | ローカル実装・検証済み（2026-09-19） | CSV・Markdown・XLSXの出力と取り込み、部分入力・未実施の扱いを実装。下記に検証結果を記録。 |
| #31 | ローカル実装・検証済み（2026-09-23） | 記入済みpayloadから標準4種類の候補を作成し、編集後に登録前検証するCLIを追加。単独CLIの登録・統合・報告まで確認。 |
| #42 | ローカル実装・検証済み（2026-09-23） | screening 4の複数条項mapping、全観測の根拠・競合保持、確認キューへの対応関係、条項数と一意な観測候補数の分離、旧run 10／11の読取りを確認。 |
| #55 | ローカル実装・検証済み（2026-09-23） | 指摘・条項・観測・改善案の多対多対応、人手の複数指摘と署名検証、件数分離、再検査申告の集計、旧単数形式の読取りを確認。 |
| #43 | ローカル実装・検証済み（2026-09-25） | 同一条項の複数確認者、一致・不一致、元の記録を残した訂正、履歴の公開設定を登録・統合・報告・入力シートへ接続。 |
| #41 | ローカル実装・検証済み（2026-09-25） | 参加観点、制約、次回確認、独立監査、資料整備を根拠付きの補足成果物として登録。統合と公開範囲を検証。 |
| #60 | ローカル実装・検証済み（2026-09-25） | 当事者のタスク観測を規格判定と分離。匿名ID、同意・保存期限、複数人の傾向、関連指摘、公開要約を検証。 |
| #48 | ローカル実装・検証済み（2026-09-25） | coreだけで手動・委託先・CMS・デザイン・通常PRの変更を申告。改善計画と根拠ファイル、前後版、同じ対象範囲の再測定を照合して新runの再検査へ接続。 |
| #49 | ローカル実装・検証済み（2026-09-25） | 再検査runと前runの指摘、条項、人手結果、screening候補、根拠、改善宣言、対象・範囲・環境を私的なJSON／Markdownで比較。ID変更は明示対応付けを使用。 |
| #59 | ローカル実装・検証済み（2026-09-25） | 指摘ごとの版付き管理記録で状態、担当、責任者、期限、リリース、外部Issue、例外判断を追跡。確認済み変更・再検査・根拠を閉鎖条件として照合し、期限・責任者・例外警告をstatus／reportへ表示。 |

#39の確認根拠は、日時・schema・証拠identity・登録／統合／report・fixerの関連178テスト（177成功、symlink権限による1スキップ）、`verify-all.mjs` の全57テストファイル成功、説明更新後のagent契約20テスト成功。CLIのエラーには入力例を表示し、旧UTC記録の読み取りと固定schemaの不変を確認した。期限判定とschema／roleの制約は独立レビューで阻害指摘なし。実ブラウザ・支援技術の動作確認は今回の日時検証には含めない。

#25では、DOM／AX tree／画像／ログ等の相対パス、SHA-256、取得日時、run・宣言対象・環境との対応を記録する。`bind-evidence` は新しい候補に参照を追加し、`compare-evidence` は証拠バイトと文脈の変更を別々に表示する。登録・検証・status・統合・報告で保存ファイルを再確認し、欠落・改変・経路逸脱を拒否する。独立レビューの比較表示に関する指摘は修正してテストを追加した。E0/E1の最低条件、旧形式の読み取り、公開範囲を手順書に明記し、サンプルも実ファイル参照へ更新した。Codex／Claude配布物の同期、パッケージ・catalog検証、`verify-all.mjs`（全58ファイル）が成功。実ブラウザ・支援技術の実機検証と実対象のdrift検出は、この成功には含めない。未push・未PR・未merge。

#54の第1段階では、fileの実バイト、GitのHEAD・index・選択ファイル、HTTPの最終URL・応答hash・ETag等、保存済みDOM／AXとviewport・locale・認証状態識別子を測定する処理を追加した。`capture-targets` はrunの対象参照ごとに1件の測定を行い、artifact root内へ非公開の補助記録を新規保存する。HTTPはrunの許可と呼出側の明示allowlistを両方要求し、DNSで検証した接続先を固定する。Gitは外部filterやfsmonitorを実行せず、サイズと所要時間を制限する。独立レビューで指摘された同一参照への複数測定は拒否するよう修正済み。

対象一覧の照合、前後のidentityと環境の別々の比較、run変更・対象変更・チェック結果の偽造／再利用を拒否する処理も追加した。専用22テスト中21成功、WindowsではPOSIXの実行権限変更テスト1件をスキップ。配布物同期、パッケージ・catalog検証、`verify-all.mjs`（全61ファイル）が成功した。実ブラウザ・支援技術・外部サイトへの接続は行っていない。

2026-09-19に#54の登録経路への接続を完了した。`capture-targets` の補助記録を `bind-targets` で初期runへ一度だけ固定し、screening／human review／authorization等のenvelopeに完全一致するsnapshot IDを要求する。登録時にfile・Git・HTTP・保存済みブラウザ状態を再測定し、変更した対象の証拠を同じrunへ追加する操作を拒否する。未計測runではE0の計画・queue・候補改善案のみを扱う。既に対象を変更した完了済みchange-recordは従前IDとauthorizationの結び付けを維持して `retest_required` へ進み、新しいretest runで再取得・再固定する。

旧run 7以前のschema／registry／envelopeを固定して読み取り互換を維持した。`compare-targets` はbefore／afterのidentityと環境をオフラインで比較し、出力を後続runの非公開artifact root内に限定する。過去記録のvalidate／status／merge／reportも対象へ再接続しない。独立レビューで見つかった、自由記述へ混入したinventory ID・hashが公開レポートに残る経路を修正し、回帰テストを追加した。サンプルと説明書、Codex／Claude配布物を更新し、追加9テスト、関連137件中136成功・symlink権限による1スキップ、`verify-all.mjs` の全62ファイル、パッケージ・catalog検証、差分検査が成功した。保存済みブラウザ状態は現在のliveページや認証済み本人の証明ではなく、HTTPの再取得も連続監視ではない。実ブラウザ・支援技術とリモートCIの実行はこの検証に含めない。未push・未PR・未merge。

2026-09-19に#65の取り込み経路を実装した。axe-core 4系のnative JSONと本パッケージのraw exportを非公開artifact rootから読み込み、version・設定・元結果のhash・対象snapshot・環境を保存する。4種類の結果、未知rule、入れ子のselector、各frameの失敗・状態変化を保持し、登録済みscreening checkとの対応候補と、tool／runをまたぐ照合キーを作る。自動結果からprofileのpass／failは生成せず、人の確認を要求する。native JSONだけではDOMとの同時取得を証明できないこと、照合キーは結果を自動削除・統合しないことを明記した。

raw結果・設定・ツール由来の自由記述を公開レポートへ直接出さず、保存後の改変も検出する。独立レビューで指摘されたviewport省略と検査配列の欠落を拒否し、実Chromeで判明した保存後のAX tree hash不一致を修正した。署名を確認したシステムChrome 152.0.7977.84、Playwright 1.62.1、axe-core 4.13.0でscanner→import→register→queue→merge→reportのE2Eが成功。関連27テスト、追加修正後の取り込み5テスト、全64テストファイル、配布物同期・パッケージ・catalog検証、差分検査が成功した。CI workflowにもE2Eを追加したが、リモートCI・支援技術実機・他scanner adapterの検証は含めない。未push・未PR・未merge。

2026-09-19に#13の検査環境確認を実装した。`preflight-web` は6能力を列挙し、別の固定fixtureで描画後DOM、AXのrole／name、Tab順、画面幅・media query、許可済みGETと拒否fetch／WebSocketの遮断を実測する。既存scanner／captureも対象のDNS解決前に同じ事前確認を行う。依存関係の存在だけを調べるDoctorとは区別し、Codex・Claude・その他Nodeホストの機能表、package／agent manifestの参照、起動手順を更新した。能力不足は未確認と次のテストを返し、実機スクリーンリーダーは常に外部確認扱い。preflight成功や台帳初期化を検査完了に変換しない。

独立レビューの指摘に基づき、通信遮断をAPIの有無だけで判断せず実測し、終了待ちにも期限を設け、強制終了を自分で起動したブラウザに限定した。実Chromeを含む関連23テストと説明・agent契約・取り込みを含む48テストが成功。最初の全体実行では、並行負荷の下で正常なChrome終了が2秒を超えて失敗したため、終了猶予を10秒に修正し、timeoutのunit testは仮想時計で検証するようにした。その後、`verify-all.mjs` の全66テストファイル一括実行、配布物同期・パッケージ・catalog検証が成功した。実機スクリーンリーダー、他ホスト固有browser tool、リモートCIの実行はこの成功に含めない。未push・未PR・未merge。

2026-09-19に#63の通信方針と実行経路を接続した。`network-policy` は対象originと収録済み一次資料URLから通信なしで候補を生成し、`init --network-policy` が具体的な許可先・GET／HEAD・転送・子フレーム・付随リソースの方針を固定する。各実行で呼出側の許可も照合し、DNSで検証したIPへ直接接続する。HTTPのcapture／bind／registerとrun指定browser scanは非公開ログを保存し、証拠への紐付け後はrun・方針・対象・生ファイルhashをオフラインで再検証する。旧run 8／registry 7を固定し、旧allowlisted宣言だけでは強制確認済みと扱わない。

実Chromeで転送・同一originのiframe・CSS取得を確認し、別originの転送、許可外通信、popup等を遮断した。通常のbrowser routeでは転送後の要求が捕捉されないことが試験で分かり、CDP Fetchによる各要求の停止・検査と、制御外通信を拒否する専用proxyに変更した。独立レビューで指摘された送信後失敗の記録と大量同時取得を修正し、許可判定と通信結果を分離、同時4件・合計32 MiB・待機64件の上限、違反時の実行中／待機中通信停止を加えた。全体テストで判明した旧版番号の期待値と、表示用の省略可能項目の互換性も修正した。専用16テスト、全70テストファイル一括実行、配布物同期・パッケージ・catalog検証が成功した。

別originのiframe、worker、認証依存ページなど、現adapterで強制や取得を確認できない経路は停止し、未確認として残す。通信ログは本人認証や監査の完了証明ではない。公開レポートには許可先の件数と方針だけを表示し、URL・IP・ログパス・方針hashは出さない。リモートCI・他ホストbrowser tool・実機支援技術の確認はこの成功に含めない。未push・未PR・未merge。

2026-09-19に#64の操作承認を実行経路へ接続した。`init --interaction-policy` に具体的な操作・禁止操作、監督者の識別子と役割、対象、開始・期限、per-run／per-actionの区分を要求する。保存済み方針や人手レビュー記録だけでは実行せず、ホストの承認UIと在席確認を接続した実行中のhandleを別に要求する。承認IDを一度限りのpreviewと結び付け、run・測定対象・操作範囲・期限の変更や承認の再利用を拒否する。旧run 9／registry 8は固定して読み取り可能に保つ。

対応browser adapterは、測定済みDOMとの一致を確認した上で、スクリプトと追加通信を停止したページのTab／Shift+Tabだけを実行する。送信・購入・公開・アップロード・アカウント変更などは既定で禁止し、展開・ダイアログ・遷移など未対応操作も入力前に拒否する。承認と操作前の状態を非公開JSONLへ同期保存し、操作後・拒否・停止理由を追記する。期限切れ・在席喪失・取消でread_onlyに戻り、開始済みの操作が中断された場合は結果を未確定として残す。ハッシュ鎖は外部署名や本人認証の代わりにならない。

独立レビューで見つかった、操作前の保存中に期限が切れる競合と取消履歴の欠落を修正した。実ChromeでTab2回、focusinによる副作用0件、送信など7操作の拒否を確認。単体10件とブラウザE2E1件、全72テストファイルの一括実行、追加したCLI入力・旧run互換性確認、配布物同期・パッケージ・catalog検証が成功した。公開モデルとレポートで監督者情報が出ないことも検証した。一般CLIは実行時の監督UIを持たないためread_onlyで、アプリ固有のキー操作、実機支援技術、他ホスト固有の制御、リモートCIは未確認。未push・未PR・未merge。次は#57／#58へ進む。

2026-09-19に#57／#58共通の署名検証基盤と、人手レビュー記録のオフラインCLIを部分実装した。正規化したレビュー全文、担当者ID、対象・規格・範囲・環境を署名に結び付け、run付きでは登録artifactの実バイトhash、測定inventory、入力artifactのhashも照合する。受領者が監査データの外で選ぶ信頼方針と別管理のhashを要求し、自己署名から本人認証へは昇格しない。担当者と署名者の不一致、別runへの流用、改変、期限切れ、失効、役割や対象範囲の不一致を拒否する。公開用の検証結果には氏名・ID・役割・所属・鍵・内部hashを含めない。

独立レビューで確認された担当者IDと署名者の結合、同一対象の別runへの署名流用を検証に追加し、再確認で重大な追加指摘はなかった。単体・CLIの追加19テスト、実ChromeのE2Eを有効にした全75テストファイル一括実行、配布物同期・パッケージ・catalog検証、差分検査が成功。テスト鍵は架空fixture用にメモリ内だけで作成し、本番鍵の生成・登録や外部署名サービスへの送信は行っていない。

2026-09-19に#57のassessment・claim guard・レポートへの接続を完了した。新しいassessment 2.0.0は一意のIDとportable reviewを保存し、`human_declared`行を署名対象へ結び付ける。単独の`human-review apply`とrun付きの`merge --review-record`で、担当者が実際に作成したレビューを新規assessmentへ反映する。本人認証フラグは保存せず、検証・報告時に元のレビューと外部信頼方針から保証水準を計算する。run付きでは元artifactの実バイトも再確認する。

自己申告・自己署名・署名者確認済み・組織の方針・独立した担当者を区別し、全員の本人性を確認できない場合のclaim上限と固定文言を制限した。旧assessment 1.0.0は凍結schemaで読み取り、`human_verified`を旧形式の自己申告と表示する。旧E4／E5には独立した担当者の本人性が未確認である旨を付記する。公開レポートでは件数と固定ラベルを使用し、氏名非表示モードで担当者名も伏せる。AIが本人認証状態を自己申告で設定する経路は設けていない。

独立レビューの指摘を受け、署名にない`review_details`の後付けと旧E4／E5の過大表示を修正し、finding本文の差替えも拒否した。再確認で高・中重大度の追加指摘はなかった。統合6テスト、run付き署名mergeからvalidate・公開Markdown／HTMLまでのCLI拡張、混在する保証水準のclaim制限が成功した。全76テストファイルの再実行は606件中599成功・1失敗・6スキップ。失敗はインストール検証中に開発用のコード索引DBが外部更新されたことによるもので、該当4テストは操作を止めた単独実行で成功した。先行実行のChrome終了確認失敗も単独実行と再実行で成功しており、未解消の失敗はない。配布物同期、パッケージ・catalog検証、差分検査も成功した。

テスト鍵は架空fixtureのメモリ内だけで生成した。本番の署名者・鍵・信頼方針の決定、実機支援技術、スキップした環境依存テスト、リモートCIは検証済みに含めない。レビュー署名は内容の正しさ、改善計画、最終報告書全体を保証しない。次は#58の最終bundle・前段署名の検証へ進む。信頼できる長期タイムスタンプの保証は別の未対応事項として明示する。未push・未PR・未merge。

### #58の監査bundleと前段署名（2026-09-19）

`audit-bundle prepare`で非公開の署名対象一覧を作り、外部で署名した記録を`verify`で再照合する。run・artifact・保存証拠・assessment・reportの実バイトと、明示した追加ファイルを含む。対象inventory・resource版とhash・署名者ID・役割・期限・前段の署名を結び付け、外部で選んだ信頼方針により未署名、自己署名、署名者確認済み、組織、独立した署名者を区別する。最低保証水準は署名鎖の全段へ適用する。現在の形式が不正なrunはartifactを開く前に拒否し、Windowsのドライブ相対パスも実行時に拒否する。

過去の実ファイルの保存、対象ソースの完全収集、レポート内容の正しさ、信頼できるタイムスタンプは保証しない。前段の署名がない履歴は連結不完全として表示する。受領者が別途確認した署名対象hashとの照合により古い有効記録への置換を拒否できるが、最新版の存在は自動検索しない。秘密鍵・本番署名者の作成、外部サービスへの送信、透明性ログへの証拠公開、適合主張の昇格は行わない。

専用7テストで1バイト改変、再計算したhashと署名の不一致、証拠の省略、誤った署名者・役割・対象・kind、期限・失効、鍵交代、前段の不足、移送後の検証、重複JSONキー、容量制限を確認した。関連49テスト成功。CLI・契約・日英ヘルプ等の132テストは旧形式fixtureの混入項目1件を修正して再検証した。全77ファイルの一括実行は613件中606成功・1失敗・6スキップ。失敗は権限拒否時の文言期待値と旧形式fixtureの不備で、修正後の該当ファイルは43成功・環境依存1スキップ、未解消の失敗はない。実Chromeの検査・取り込み・能力確認・監督付き操作を含む。配布物同期、178共有ファイル・306 JSONのパッケージ検証、catalog検証が成功した。

独立レビューの暫定指摘は別々の読込cacheを同一と扱ったもので、再確認で撤回され、高・中重大度の未解消指摘はない。テスト鍵は架空fixtureのメモリ内だけで生成した。本番の鍵・署名者・信頼方針、支援技術実機、スキップした検査、リモートCIは検証済みに含めない。未push・未PR・未merge。次は#50、#35へ進む。

### #50の出典・利用条件・生成物への帰属表示（2026-09-19）

8つの出典記録に採用版・URL・提供者・保存項目・加工内容・利用条件・確認日を保存した。W3CのDocument LicenseとSoftware and Document Licenseの2015／2023年版を区別し、利用条件全文を元のバイトとhashで同梱した。WAICのCC BY-SA 2.1 JP、デジタル庁の公共データ利用規約に基づく出典・加工表示を記録し、参照のみの未確認資料をMITで補完しない。法的レビューは未実施とし、混合データへの継承条件の適用など、具体的な利用方法に応じた確認が残ることを明記した。

機械可読台帳からrootと配布スキルのnoticeを生成し、Markdown／HTMLの本文・要約・付録へ帰属表示を引き継ぐ。カタログの更新候補には実バイトのhashと出典、notice、再確認待ち状態を記録した `.sources.json` を添える。package／catalog検証は、必須出典・binding・表示指定の欠落、資料や利用条件全文の変更、noticeの不一致を拒否する。独立レビューで判明した表示無効化・binding削除・追加出典の表示漏れを修正し、回帰テストを追加した。

専用7テスト成功。全78テストファイルの一括実行は620件中614成功・失敗0・6スキップで、実Chromeのscanner・取り込み・能力確認・監督付き操作を含む。スキップされたHTMLレポート検査をシステムChrome指定に対応させて追加実行し、日英10種類のHTML、axe、キーボード、320px表示の検査が成功した。残る5件はsymlink権限などの環境依存検査。配布物同期、185共有ファイル・310 JSONのpackage検証、catalog検証が成功した。

既存catalog・registry・ARIAルール・procedure・failure-patternのJSONは変更せず、監査runのresource hashも変更していない。実機支援技術、リモートCI、商用利用・再配布の法的確認は成功に含めない。未push・未PR・未merge。次は#35へ進む。

### #35の保守導線と配布候補（2026-09-19）

`f9559c9`で対応版・移行方針、SECURITY／CONTRIBUTING／CHANGELOG、4種類のIssueフォームとPR template、READMEの案内を整備した。package 0.1.0は未公開の開発系列としてcommit SHAも記録し、run 10／registry 9／envelope 3、assessment 2等の現行版と旧形式の読取り条件を明記した。破壊的変更を版番号の書換えだけで移行せず、元の証拠・署名とpackageを保持する。

`build-release.mjs`はcleanなHEADから、`release-files.json`で確認した532ファイルだけを収録する。完全なcommit、アーカイブと各ファイルのSHA-256、versioned release noteを付け、既存出力は上書きしない。GitのWindows改行変換を固定し、一時展開後に全ファイルの実バイトとpackageを検証する。研究用原本・非公開audit-runs・Git履歴・node_modules・内部作業記録は除外する。独立レビューで指摘された未知のfixture／example混入を、収録一覧の完全照合とnegative testで修正した。既存ファイルの内容については、公開前のdiff reviewが引き続き必要。

実際のcommit `f9559c9a3ff1cc4843e41d95dc187739024a0153`から、`audit-runs/issue35-release-candidate/`へtar.gz、source-manifest.json、release-notes.md、SHA256SUMSを生成した。tar.gzは1,141,202バイト。展開後の185共有ファイル・161 JSONのpackage検証、catalog検証、Claude dry-run、64-bit Windows PowerShellでのCodex WhatIfが成功し、導入先ディレクトリは作成されていない。候補はこのcommitに固定され、後続の進捗文書commitを取り込んだと扱わない。

専用3テストで全ファイルhash、再現可能なアーカイブ、上書き・未commit変更・禁止パス・未知ファイルの拒否を確認した。README・NVDA smoke契約を含む関連検証が成功。全79テストファイルは623件中618成功・失敗0・環境依存5スキップで、実ChromeのHTMLレポート、scanner、取り込み、能力確認、操作承認のE2Eを含む。実機支援技術とリモートCIはこの成功に含めない。

| #35の受け入れ条件 | 現在の状態 |
| --- | --- |
| 非公開の脆弱性報告先 | 具体的URLとfallbackを案内済み。GitHubの機能は未有効化で、設定適用の確認回答待ち。 |
| 対応package／schema版 | `docs/version-support.md`へ現行版・旧形式・移行制限を記載し、ローカル検証済み。 |
| 貢献者向けの同期・検証・一次資料確認 | 正本・生成先、sync／verify、利用条件の再確認手順を更新済み。 |
| Issueフォーム | bug、docs/usability、standards update、featureで版・対象・根拠・公開範囲を収集。GitHubへの反映は未実施。 |
| 破壊的変更・移行のCHANGELOG | run・assessment・screeningの変更と、移行ツール未提供を記載済み。 |
| release artifact／note | ローカル候補と展開後の検証は完了。tag・GitHub Releaseは未作成・未公開。 |
| repository topics／About | 具体的な設定案を用意。API再確認ではtopicsは空、homepage未設定。設定適用の確認回答待ち。 |
| READMEの保守文書へのリンク | 日英READMEへ対応版・リリース手順を追加し、リンクと構造の検証成功。 |

設定変更の確認は、非公開報告の有効化とAboutのREADME URL・6 topicsだけを対象としている。push・PR・tag・Release公開の承認とは扱わない。#35はこれらの外部反映が残るため未完了のまま保持し、実行可能な次の#16へ進む。外部操作待ちは他の未着手Issueを止める理由にしない。

### #16：対象箇所と観測に結び付いた人手確認queue

2026-09-19に `725be0f` へローカル実装を保存した。run 11／registry 10／queue 3で、対象snapshot・画面や要素・必要な状態、登録済み観測のartifact IDとscreening requirement ID、確認理由、優先度と理由、影響を確認する利用者を保持する。同じ基準の複数箇所・観測は一項目に集約する。別run・未登録・別基準の観測、対象の取り違え、同一箇所・状態の重複、入力内の観測の欠落を拒否する。

`review-queue` は登録済み観測、全基準、明示した基準から非公開の候補を生成し、初期優先度を未設定にする。全基準モードは全登録基準と全宣言対象snapshotを含む。候補作成だけではrunを変更せず、人手確認の結果も作らない。状態は未確認または前提条件待ちに限定する。独立レビューで見つかった観測前の候補が登録できない不整合を修正し、initializedからqueue登録へ進む遷移を現行registryだけに追加した。再レビューで高・中重要度の追加指摘なし。

日英Markdown／HTMLの本文・要約・別紙に確認箇所、必要な状態、優先度と理由を表示し、内部artifact ID・snapshot ID・元証拠パスを公開モデルへ渡さない。自由記述には公開用の伏字処理を適用する。run 10／registry 9／queue 2を元のバイトから凍結し、従来の対象・権限・人手binding検証を維持した。run 10の既存assessmentはレポート再生成に対応し、書込み操作は現行runに限定する。さらに古いrunのレポートは元の対応packageを使用する。旧queueの不足情報を推測して補完する自動移行は行わない。

| #16の受け入れ条件 | ローカルの確認結果 |
| --- | --- |
| 各項目から対象箇所へ到達 | 同一run・envelopeのsnapshot、宣言対象参照、箇所と必要状態を必須化。 |
| 観測との正確な関係 | 登録入力のartifact ID＋観測のrequirement IDを検証し、基準・箇所・証拠対象も照合。 |
| 観測／全基準／手動の区別 | originsに明示。全基準は全登録基準・全対象を検証。 |
| 優先度と理由 | P0〜P3／未設定と理由を必須化し、公開レポートにも表示。 |
| 対象・基準の重複検出 | 同一基準の別項目と同一snapshot・箇所・状態を拒否。 |
| 公開時の人向け箇所表示 | 日英・2形式・本文／要約／別紙で確認し、内部IDの不在を検査。 |
| 旧形式の方針 | 凍結schemaのhashと旧run 10のvalidate／report成功、旧形式での候補作成拒否を確認。 |
| binding／E2E更新 | 専用5テスト、登録・統合・サンプル・scanner取り込み・公開レポートを更新。 |

全80テストファイルは628件中606成功・17失敗・5スキップで、失敗はサンプルのimportとshebangの配置、版番号、新遷移の期待値の3原因だった。修正後、失敗した経路を含む関連120件がすべて成功した。複数対象の全基準確認と凍結hashの追加後も関連13件、サンプル整理後のE2Eが成功。実Chromeのscanner・取り込み・能力確認・通信制御・操作承認・HTML検証、Codex／Claude配布同期、192共有ファイルのpackage検証、catalog、差分検査が成功し、未解消のテスト失敗はない。実機支援技術とリモートCIは未実行。

箇所名の意味的な同一性や異なるselectorの同値性は自動判定しない。初期値の「対象ページ全体」や未設定の優先度は、確認者による具体化が必要。再登録・複数評価者の調停・指摘の状態管理は後続Issueの範囲として保持する。未push・未PR・未merge。次は#17のワークシートと取り込み、その後#31の残るartifact作成支援へ進む。

### #17：人手レビューのワークシートと取り込み

2026-09-19に `d0bbedb` へローカル実装を保存した。`human-review export` は登録済みqueueからCSV・Markdown・XLSXを作成し、`human-review import` は確認者の回答を `declared-human-review` の候補へ変換する。基準名、対象箇所と状態、手順、一次資料、必要な証拠を固定欄にし、判定・理由・証拠・不適合の情報を入力欄に分けた。候補作成時点ではrunを変更せず、登録は既存の `register` で行う。

| #17の受け入れ条件 | ローカルの確認結果 |
| --- | --- |
| queueからワークシートを作成 | CSV・Markdown・XLSXの3形式をCLIで出力し、同じ入力項目を使用。 |
| 基準名・箇所・手順・一次資料 | 登録済みqueueと現在のcatalogから表示し、取り込み時に固定欄を照合。 |
| 5種類の判定 | pass／fail／not_applicable／not_tested／cant_tellを検証。空欄は未提出。 |
| failの必須情報 | 理由・必要証拠に加え、優先度・箇所・影響を受ける利用者・観測内容を要求し、指摘IDを生成。 |
| 記入済みシートのartifact化 | 3形式の取り込みから登録・評価統合まで成功。既存ファイルは上書きしない。 |
| ID・hash・手順・日時 | run／queueの実バイトhashを照合し、run ID・入力hash・手順参照・作成日時をCLIで設定。証拠の時差と秒未満を保持してUTCへ変換。 |
| 一部提出・複数確認者・再提出 | 基準単位の一部提出と、担当が重複しない確認者の追加を検証。更新後のrunでは再出力を要求し、登録済みの基準を除外。訂正と競合の制限を文書化。 |
| 改変・古い入力・別runの拒否 | 固定欄・行・hashの改変、更新前のシート、別run、記入漏れ、無効日時、数式・外部参照・過大なXLSXを拒否。 |

独立レビューを受け、現行run 11の `not_tested` は未実施を申告する `manual_observation` のみに限定した。実施していないブラウザー・キーボード・支援技術の証拠は要求せず、未実施だけの記録でE2へ昇格させない。統合と日英レポートでも未実施を実施済み件数から除外し、未確認queueに保持する。旧run 10と単独assessmentの従来の証拠検証は維持した。限定再レビューで高・中重要度の残指摘なし。

ExcelJS 4.4.0を固定依存に追加し、XLSX入力は展開前の容量・ZIP構造・危険な内容を検証する。実Excel 16.0を64-bit Windows PowerShell COMで開き、合成回答の入力・再保存・取り込みに成功した。Excel標準の圧縮フラグに対応し、暗号化は引き続き拒否する。PDFプレビューで固定欄と入力欄、折返し、ページごとの見出しを確認した。シート保護は編集補助であり、本人確認・署名とは扱わない。

全81テストファイルは638件中633成功・失敗0・5スキップ。日英の未実施レポートと実施件数の検証を追加した後も、専用9テストが成功した。実Chromeの既存E2E、配布同期、196共有ファイルのpackage検証、catalog、差分検査が成功した。導入済み依存ライブラリのJSONCでpackage検証が失敗したため、依存フォルダ・非公開run出力・Git管理領域をソースJSON検査から除外し、通常ソースの不正JSONは拒否するテストを加えた。リリース対象は審査済みの9ファイルを明示追加して556ファイル。日本語ガイドの語彙チェック2件は、技術上の「検査」と「入力漏れ」という必要な用法として維持した。

本人性の認証、実際の観測内容の正しさ、実機支援技術によるワークシート操作、リモートCIは未確認。登録済みレビューの差替えと同一基準の複数評価者の調停は後続Issueに残る。未push・未PR・未merge。次は#31の残る標準artifact作成支援と単独CLI完走へ進む。

### #31：標準artifactの作成と登録前検証

2026-09-23に `artifact init`／`artifact validate` を追加した。記入済みpayloadからscreening・queue・人手レビュー・改善計画の候補を作り、ID・版・run・producer・UTC日時・登録入力のhashをCLIが設定する。既存のqueue作成とワークシート取り込みも利用できる。空のテンプレートへ架空の観測や人手判断を入れず、`--payload` を必須とした。修正の承認・実行記録は既存の専用経路を使う。

新規の一連の操作テストで4種類の作成・編集・検証・登録・統合・レポート出力を確認し、別run・古いhash・未登録入力・改変済み入力・上書き・出力先の逸脱を拒否した。既存のワークシート・登録・統合CLI関連87テストも成功。Codex／Claudeの197共通ファイルと配布対象一覧を同期し、パッケージ検証が成功した。新しい依存ライブラリ・schema・大量のfixtureは追加していない。全件テストと実ブラウザ検査は今回の変更に不要なため再実行していない。

人手レビューのproducerは申告元を表し、CLI実行者の本人性や実施事実を証明しない。本人が作成した宣言のみを渡す要件を説明に追加した。元の作業ディレクトリは別タスクのブランチになっていたため、`e753849` を起点とする独立worktreeの `codex/artifact-authoring-issue31` へ保存する。未push・未PR・未merge。次は#42の観測と条項の複数対応・競合保持。

### #42：複数観測の根拠保持（第1段階）

同一条項の観測をseverity順で1件に絞る処理を、全件の箇所・方法・根拠・確認事項を保持する処理へ変更した。判定候補や適用判断が一致しない場合はレポート上で `cant_tell` と競合理由を表示し、生成する人手確認キューへも引き継ぐ。人手レビューの優先と、未完了検査をpassにしない既存guardを維持した。登録済みの元ファイルは変更しない。

関連42テストを実行し、追加表示の伏字漏れ2件と表示形式の期待値1件を修正・再検証した。新しいCLIテストではpass／fail／not_applicableの3観測を同一条項へ登録し、queue・merge・日本語公開Markdown・英語内部HTMLで全根拠と競合理由を確認した。全観測が適用対象外で一致する場合も別枠へ保持する。新しいschema・依存・ファイルは追加していない。

第1段階では複数条項の保存と件数分離を残していた。以下の第2段階で#42の残る範囲を実装した。

### #42：複数条項の保存と件数分離（第2段階）

run 12／registry 11／screening 4で、観測の `profile_mappings` に条項別の判定候補・適用判断・理由を保存する。観測ID、箇所、保存済み証拠は一度だけ保持し、queueでは関連する各条項へ同じ観測を結び付ける。重複する観測ID・条項ID、異なるprofileの条項、単数形と配列の混在を登録前に拒否する。scanner取り込みも一つの元結果から一つの観測を作成する。

レポートでは観測記録数、人手未確認の問題候補数、関連条項数、競合条項数を分ける。問題候補数は `candidate_issue` またはfail mappingを持つ観測IDの一意数であり、異なる説明が同じ実際の障壁を指すかは自動判定しない。確認済み不適合の数でもない。旧run 11／registry 10／screening 3の契約を凍結し、旧run 10・11のレポート出力を検証した。登録済み証拠・署名の自動変換は行わない。

新しいmany-to-many fixtureでは、2観測・2条項・4対応から、証拠2参照、確認候補2行、観測記録2件、改善候補1件となり、4つの理由と競合が日本語Markdown・英語HTMLへ残ることを確認した。既存契約107テスト、638件の一括検証と失敗範囲の修正後再検証、Chromeでの証拠取得、インストーラー15テスト、配布物201共有ファイル・178 JSONの検証が成功した。独立レビューと互換テストで検出した旧run 11のレポート入口の拒否を修正した。

一括検証でスキップされた15件、実機支援技術、リモートCIは検証済みに含めない。PowerShellのモジュール探索先と一時依存参照による検証環境の問題は、対象テストを環境修正後に再実行して解消した。新しいproductionモジュール・依存・テストファイルは追加せず、互換性保持用の4ファイルとClaude配布側の同じ4ファイルだけを追加した。未push・未PR・未merge。次は#55の指摘・改善計画の複数対応、その後#43の人手調停。

### #55：指摘・改善案と複数条項・観測の対応（2026-09-23）

run 13／registry 12、human review 2、remediation plan 3を実装した。一つの指摘を複数条項・観測へ結び付け、一つの条項に複数の人手指摘を保持する。改善案は指摘IDを参照する別項目とし、条項ごとの複製を不要にした。未計画の指摘も保持し、レポートでは指摘件数と不適合申告の条項数を分ける。IDや参照先の重複・不一致、同一条項内の同内容の人手指摘、同じ対応関係と内容を持つ改善指摘・改善案の重複を拒否する。異なる説明や基準が同じ実際の障壁を指すかは自動判定しない。

人手の指摘ID・本文・箇所・影響・優先度を改善計画から書き換えられない。portable human review record 2の署名対象に複数指摘を含め、指摘に関連する条項の集合も署名済みレビューと照合する。旧review record 1の署名と旧run 12／単数remediation 2の読取りを保持する。登録済み証拠と署名の自動変換は行わない。

`status --retest-of` は、前回の指摘IDごと・条項ごとに後続runの人手申告を表示する。関連条項の申告を集計した状態であり、個々の箇所の修正完了や指摘の解決を認定しない。解決・例外・期限などの運用は後続の#48→#49→#59で扱う。

2観測・3条項・2指摘・2改善案を登録し、同一条項に2指摘を残すCLI一連の操作と日英レポートを確認した。署名付き複数指摘、未記録条項や本文の改変拒否、未計画指摘、旧形式の改善案の保持も検証した。全体628テストは602成功・11失敗・15スキップで、失敗は集計情報が省略されたレポートモデルの同一表示エラーだった。修正後の関連29テストは全件成功。追加修正の重点2テスト、導入15テスト、署名済みChromeでの日英HTML・axe・キーボード・320px表示の1テストも成功した。スキップ15件のうちHTMLブラウザ検証1件はこの追加実行で確認し、残る14件、実機支援技術、リモートCIは未確認とする。

独立レビューで検出した同一内容の人手指摘の別ID登録を修正した。配布物207共有ファイル・190 JSONを検証済み。新しいproductionモジュール・依存・テストファイルは追加せず、凍結互換用6ファイルとClaude側の同じ6ファイルを追加した。未push・未PR・未merge。#55はローカル実装・検証済みとし、次は#43の複数評価者の調停を進める。

## #43 複数確認者の一致・不一致と訂正履歴

2026-09-25にrun 14／registry 13、human reviewとportable review record 3を実装した。同じ条項を複数の確認者が記録でき、各レビューの確認者・役割・確認日・証拠・手順・判定を保持する。有効な全レビューが一致した場合だけその判定を集約し、不一致は `cant_tell` として確認キューと次の作業に残す。重大度や最終登録順で結果を決めない。確認者IDは申告値であり、本人認証や独立性の証明とは区別する。

訂正は同じ確認者・条項の以前の `review_id` を `supersedes_review_id` で参照する。元の記録を保持し、循環・分岐・他人のレビューの訂正・同じ確認者の二重有効記録を拒否する。署名対象にIDと訂正関係を含め、検証済みの保証水準を下げる訂正も拒否する。単独assessmentとrun付き監査の両方で同じ集約処理を使い、statusと再確認結果もそれに従う。旧run 13、review／record 1・2の読取りと旧署名を維持する。

`human-review export/import --reviews all` で追加の確認者と訂正を入力できる。公開Markdown／HTMLでは履歴を示し、`--reviewer-disclosure redact` で氏名と役割を本文・指摘詳細・確認履歴から隠す。確認者が証拠・理由を再確認して訂正する合意方式を採用し、別の調停者による強制上書きは追加しない。改善計画の登録後や対象変更後は新しいrunで再確認する。

全体630テスト（導入テストを除く）は612成功・3失敗・15スキップ。失敗は新形式のエラー文言と確認者IDに追従していないfixtureで、修正後の関連34テストが全件成功した。不一致を確認キューに残す追加修正も重点2テストで成功。導入15テスト、配布物213共有ファイル・200 JSON、catalogと差分検査が成功した。入力シートから登録・統合・status・公開HTMLまで、署名・旧形式との混在、日英レポートの非表示設定、元ファイルの不変性を確認した。ブラウザー等のスキップ15件、実機支援技術、リモートCIは今回未確認。依存ライブラリ・テストファイルは増やさず、共通処理1モジュールと凍結互換用5ファイルを各配布先に追加した。未push・未PR・未merge。次は#41のrun補足情報を進める。

## #41 参加観点・監査制約・次回確認の登録

2026-09-25にrun 15／registry 14と `audit-context` 1.0.0を追加した。参加の5観点、対象固有の制約、次回確認の日付・担当・条件、独立監査、法務・調達資料の整備を申告ごとに保存する。外部の人と依頼者の申告役割を分け、対象snapshotと保存した根拠ファイルの実バイト・SHA-256、参照する登録済み成果物を照合する。同じ観点や予定の競合は後勝ちにせず拒否し、補足情報の登録だけではrunの状態や証拠レベルを昇格させない。

mergeとreportは登録済みの申告とassessmentの完全一致を確認する。公開レポートには公開指定の参加結果、次回確認日・条件、監査と資料整備の申告有無を示し、内部指定の制約文、担当者名、根拠パスと詳細は除く。保存バイトの整合は申告内容の真実性や本人性の証明ではない。run 14／registry 13を凍結して旧版の読取りを維持した。

全体631テスト（導入テストを除く）の初回実行は613成功・3失敗・15スキップ。3件は版の期待値、未実施レビューの扱い、配布一覧の漏れで、修正後の該当テストが成功した。関連189件中188成功・1スキップ、導入15件成功。配布物とcatalogの検証も成功した。実機支援技術とリモートCIは未確認。未push・未PR・未merge。次は#60の当事者テストを進める。

## #60 当事者による利用テスト

2026-09-25にrun 16／registry 15と `participant-usability-observation` 1.0.0を追加した。1人の1タスクを単位に、仮名ID、必要最小限の利用条件、journeyと5つの参加観点、完了状況・所要時間・支援の有無、行動・障壁・回避策、同意・引用／録画権限・保存期限、共通テーマを保存する。同じrunで登録済みの人手指摘・改善案へ結び付けられる。根拠ファイルの現在のバイトと対象snapshotを照合し、別run、重複セッション、無効な同意、連絡先らしい自由文、未登録の関連IDを拒否する。

当事者観測だけではassessmentの条項結果・証拠レベル・run状態を変更しない。公開レポートは公開集計への同意と伏字確認があり、保存期限内の記録に限定する。テーマは複数人に共通する場合だけ人数とともに表示し、仮名ID・発話・録画・個別観測・根拠パスは出さない。本人性や同意内容そのものは自動認証できず、氏名等の個人情報も完全には検出できないため、公開前の人手確認を必要とする。run 15／registry 14は凍結して読取り互換を維持した。

導入テストを除く全体632件は617成功・失敗0・環境依存15スキップ。導入15件も全件成功した。候補作成CLI、登録・統合・公開HTML、非同意・連絡先・別run・保存証拠改変の拒否を実例で確認した。配布物225共有ファイル・216 JSON、catalogと差分を検証済み。実機支援技術とリモートCIは未確認。依存・新規テストファイルは増やさず、共通処理1モジュールと凍結互換用3ファイルを各配布先に追加した。未push・未PR・未merge。次は#48の外部修正後の再検査を進める。

## #48 外部変更の申告と再検査

2026-09-25にrun 17／registry 16と `declared-change-record` 1.0.0を追加した。対象への書込みを許可しないcore runでも、改善計画を入力として、手動・委託先・CMS・デザイン・通常PRでの変更を外部の人または依頼者が申告できる。申告には変更者と確認者、前後版、関連改善ID、保存済み根拠、変更後の実測inventoryを含める。申告候補の作成は `artifact init`、変更後の測定は `capture-targets --after-version` で行う。

登録時に対象を再測定し、元runと同じ対象範囲・異なるidentity・変更後版の一致を確認する。保存済みWeb状態は元のbundleを保持して別のcaptureで測定し、内容・応答の変化を要求する。対象に変更がない場合、別の対象を測った場合、登録までに対象が戻った場合、AI名義へのすり替え、根拠や関連改善IDの不整合を拒否する。登録後は `retest_required` となり、`retest` で新runを作成して対象を改めて固定する。既存の承認付きfixer経路は独立して維持する。変更者名と根拠の内容は自己申告であり、本人性や実際の作業主体を自動認証しない。対象identityの変化は改善の成功を意味しない。run 16／registry 15は凍結して読取り互換を維持した。

本体628テストは613成功・失敗0・環境依存15スキップ。導入20テストも全件成功。手動・委託先・通常PRのcore-only経路、同一状態・別対象・登録直前の巻戻し・AI名義偽装・未登録版の拒否を確認した。Codex／Claudeの配布物231共有ファイル・224 JSON、criteria catalog、差分検査が成功。実機支援技術とリモートCIは未確認。未push・未PR・未merge。次は#49の改善前後比較を進める。

## #49 再検査の差分

2026-09-25に `compare-runs` を追加した。`supersedes_run_id` でつながる旧runと新runを検証し、旧指摘ごとの `resolved`／`unresolved`／`not_retested`、新規・回帰指摘、条項別の人手結果、screening候補を別々に示す。IDが変わった指摘・候補は利用者のJSON mappingで対応付け、未対応の新IDは新規として残す。根拠ファイルの前後hashと文脈、改善計画に対する変更申告、対象版・実測inventory・scope・環境も表示する。JSON deltaとMarkdown reportは新runの私的なartifact root内へ新規作成し、旧run・新runを変更しない。

新runに確認がない指摘は解消としない。旧指摘の関連条項がすべて人手でpassと申告された場合の `resolved` も、元の各箇所の修復証明ではない。screeningの信号消失は `improved_unverified` とし、規格判定へ転用しない。対象範囲・profileが異なる場合は比較不能と表示する。run schema／registryを増やさず、比較用CLIと共通判定モジュールのみ追加した。

分類・ID対応・保存済み根拠・改善宣言の専用2テスト、実runの `compare-runs` を含む#48 end-to-end 1テスト、既存の対象比較17テスト、CLI発見と配布同期の38テストが成功。配布物233共有ファイル・224 JSONのpackage検証成功。新しい支援技術の実測やリモートCIは未実施。未push・未PR・未merge。次は#59の指摘状態・期限・例外を進める。

## #59 指摘の状態・期限・例外

2026-09-25に、登録済み指摘ごとの私的な `finding-lifecycle` 1.0.0を追加した。`lifecycle init／advance` は版ごとに別ファイルを作り、前版のhashを固定する。標準の状態遷移と担当者・説明責任者・期限・対象リリース・依存先・外部Issue対応を保持する。誤検出、重複、対象外、延期、リスク受容、例外承認を作業状態と別に記録し、承認者・理由、期限と再確認日を検証する。リスク受容や例外は規格のpassや指摘解消に変換しない。

`verified／closed` には旧runの登録済み変更記録、新runの再検査、関連条項すべての人手pass申告、保存済み確認根拠を要求する。`status --lifecycle` は期限超過、P0／P1の責任者未設定、期限切れ例外・再確認日到来を表示する。`report --lifecycle` は状態・期限・警告・判断種別だけを公開要約へ追加し、担当者名・承認者名・外部Issue ID・根拠パスを除外する。ライフサイクルの指定がない既存status／reportの出力は変更しない。管理記録はrunの私的な補助ファイルであり、run／registryのschemaは増やしていない。

実runでのopenからclosedまでの再検査・根拠照合、無効遷移、期限不足、前版改変、公開情報の除外を専用テストで確認した。既存status／reportの26テスト、配布物237共有ファイル・226 JSONの同期検証、criteria catalog検証が成功。支援技術実機とリモートCIは未確認。未push・未PR・未merge。次は#53のAI修正引継ぎと実行主体の分離を進める。

## #53 AI引継ぎと実行者の分離（ローカル実装・対象検証済み）

旧run 17／registry 16では `authorized_fixer` が `ai_agent` かつ唯一の対象書込みroleであり、runtimeのchange-recordにもそのproducerが記録される。まず旧記録を読む際に、producer欄がAI引継ぎroleであって実行者の証明ではないことをstatusとreportに明示し、agent指示も修正した。旧schemaと保存済みrunのhashは変更していない。実行・登録済みの旧change-recordを使ったstatus確認、既存status／reportの26テスト、agent／配布同期の41テスト（39成功・環境依存2スキップ）を検証した。

registry 17／envelope 4でAIの `authorized_fixer` を書込み不可の `fix-handoff` 作成roleとし、実行runtimeを唯一の書込みrole `trusted_fix_executor` に分離した。`fix-handoff` は外部認可と改善計画の登録済みhash、変更対象・前後hash・検証commandを固定する。`change-record` 3はhandoff、宣言されたoperator ID、runtime／command brokerのhash、実行時刻、実測の前後hashを結び、登録時にrun内のレシートとホスト側の完了レシートを照合する。operator IDは本人認証ではなく、同一アカウントの管理者を防ぐ証明でもない。

旧registry 16／envelope 3／change-record 2を凍結して読取りを保持し、旧runへの新規登録を拒否する。AI引継ぎだけでは対象が変わらないこと、create／modify／delete、rollbackと認可再利用拒否、手書き変更記録の登録拒否、旧記録の状態表示、現行runのstatusと配布同期を対象テストで確認した。パッケージ検証とcatalog照合も成功。一括テストはこのworktreeに `exceljs` がないため `human-review-worksheet.test.mjs` のimportで停止し、全テスト成功とは記録しない。未push・未PR・未merge。次は#33の任意fixer分離を進める。

## #33 任意authorized fixerの導入境界（ローカル実装・対象検証済み）

Codex／Claudeの既定installerは監査専用skillを配置し、修正用agent・CLI・実行module・payload schemaの15ファイルを省く。Codexの`-IncludeAuthorizedFixer`とClaudeの`--include-authorized-fixer`は、agentと15ファイルを同じ導入処理で追加する。npmの標準CLI packageも同じ15ファイルを除外する。監査専用runtimeは修正schemaが全て存在しない構成を許し、一部だけある不完全な構成は拒否する。両構成のregistry 17.0.0との対応をfeature manifestで固定した。

監査専用のClaude導入からrunを作成・検証し、同じrunが全機能構成でも検証できることを確認した。Codex既定・任意導入とrollback、Claude既定・任意導入、npm pack内容、配布同期、関連する監査・fixer・README・releaseの対象テストが成功した。修正用schema・CLIを省いても監査runを使えるが、修正artifactを含むrunの読取りには対応版の修正機能が必要。ソースリポジトリとローカルrelease archiveは両構成の元ファイルを保つ方式で、独立した2つのソースpackageへの移動は行っていない。リモートCIは未確認。未push・未PR・未merge。次は#28・#26・#27の既存記録と導入更新を確認する。

## #28 旧runのread-only検証と再レポート（ローカル実装・対象検証済み）

保存済み旧skillの`references/`を`validate-run`／`report`の`--historical-resources`へ明示指定できるようにした。catalog・criterion procedures・audit methodsはrunに記録された実バイトSHA-256と一致する場合だけ読取りに使い、orchestration registryは同梱した凍結版のhashを照合する。standards registryは元runに版番号しかないため、ハッシュ認証できない制限をレポートと対応版文書に明示した。source run、artifact、assessmentは書き換えず、現行validatorが認める内容だけ現行rendererで再出力する。

凍結run 1・5・16の代表例で再レポート、run 16のCLIでread-only検証を確認し、指定resourceのhash不一致は拒否した。旧版の互換表とコマンド例を`docs/version-support.md`へ記録した。関連する監査・レポート・CLI・配布・READMEの142テストは140成功・失敗0・環境依存2スキップ。パッケージ同期とcatalog照合も成功。`migrate-run`と自動変換は提供しないため、元runの新版への移行や未保存resourceの復元は残課題。全体テストは依存`exceljs`欠落で未確認。未push・未PR・未merge。次は#26の非公開成果物の置き場所を確認する。

## #26 非公開成果物の出力先（ローカル整備・対象検証済み）

`audit-runs/<run-id>/`を内部成果物と公開候補の推奨先としてCLIヘルプ・skill・レポート例に統一した。確認後の公開用レポートはリポジトリ外の新しいpathへ明示的に書き出し、内部用伏字manifestはrun内の新しいpathへ保存する。run単位の保管・保存期間・手動削除の方針も記載した。既存のCLI上書き防止と`.gitignore`は維持した。3種類の内部pathが`git check-ignore`で無視されること、関連50テスト中48成功・2環境依存スキップ、配布同期・package検証成功を確認。GitHub Issueはopen、未push・未PR・未merge。次は#27の各環境の導入・更新・削除経路を確認する。

## #27 3 OSの導入・更新・削除（ローカル実装・対象検証済み）

既存のClaude Node installerを共通化し、CodexのNode入口を追加した。両者ともdry-run、既存skill・manifest管理agentを新規backupへ退避してからの更新、退避型uninstall、以前のbackupからのrestoreを利用できる。新規導入は既存配置先を拒否し、更新中の失敗では退避したファイルを戻す。任意fixerは既定では入れず、同じfeature manifestとregistry版を照合する。既存のWindows PowerShell installerは維持した。版固定、Codex／Claudeの差、3 OSの手順、導入後のCLI／doctor確認をdocs/installing.mdとREADME日英に記載。release収録リストに新しいscript・test・文書を登録した。

Windows上の一時homeでCodex／Claudeの初回導入、更新、uninstall、restoreとinstalled CLI／doctorを確認した。macOS／Linux上の実機動作、リモートCI、公開済みtag付きreleaseは未確認。GitHub Issueはopen、未push・未PR・未merge。次はP3の#14→#32の条項手順と状態付きUIへ進む。

## #14 条項固有手順の段階拡充（進行中）

Issue作成時の4件は後続作業で8件になっていた。まずW3C一次資料のWCAG 2.2 SC 2.4.11（focusが隠れないこと）と3.3.1（入力誤りの特定）を照合し、適用条件・操作・期待結果・証拠・cant_tell・反例・人とAIの境界を英日で追加した。profile一覧は既存のrequirement indexと同じdirect/equivalent判定から手順件数と割合を表示する。この段階のcoverageはWCAG 10/55（18.2%）、JIS 9/38（23.7%）、旧組合せ9/56（16.1%）。カバレッジ下限の回帰テストを追加した。

続いてSC 1.4.10（reflow）と4.1.3（状態メッセージ）をW3C一次資料と照合し、英日手順を追加した。reflowは縦スクロール内容の幅320 CSS px相当と横スクロール内容の高さ256 CSS px相当、二方向配置の例外を必要部分に限る境界を記録する。状態メッセージは文脈変更との区別、role／propertyでの公開、実際の読み上げとの証拠の分離を記録する。WCAGのcoverageは12/55（21.8%）、JISは9/38（23.7%）、旧組合せは9/56（16.1%）。

関連する28テストと出典台帳・配布物・パッケージ検証が成功した。残りの条項固有手順は未整備で、#14は完了扱いにしない。一度に全条項を増やすと配布物と保守量が大きくなるため、利用頻度と監査上の影響を見て次の小単位を選ぶ。関連する#32は利用頻度の高いUIチェックを先に拡充した。GitHub Issueはopen、未push・未PR・未merge。

## #32 状態付きUIチェックリスト（ローカル実装・対象検証済み）

固定のpattern ID一覧をスクリプトとschemaから外し、`--list-patterns`で現行一覧を表示できるようにした。W3C一次資料に基づきtabs、combobox、status messagesを英日で追加し、表示・操作・accessibility tree・focus・実際の読み上げを別々の確認項目として記録する。任意の`--extension <file.json>`は同じschemaのpattern形状で検証し、IDの衝突や出典URLの不備を拒否する。日本語表示でも拡張本文は英語のままと明示する。`all`の対象は同梱と読み込んだ拡張だけで、未掲載UIを評価済みとみなさない。

関連するCLI・localeの28テストが成功。Codex／Claudeの共有ファイルを同期した。実機の読み上げ、未掲載パターン、リモートCIは未確認。GitHub Issueはopen、未push・未PR・未merge。次は#14の残りから小さな単位で再開する。

## #66 ATAGの入口を参照ガイダンスに統一

Issueの選択肢2を採用した。ATAGを理由にskillは起動するが、inactiveな`authoring-agent`を規格評価profileとして案内しない。ATAG Part Bは名前を付けた制作工程の参照ガイダンスとして扱い、Part Aのhost UI、Part B内の作者支援機能と自動生成物を区別する。Web WCAG profileへの代用やATAG適合報告をしない。registryの`formal_conformance_target`は将来対象の記述であり、現行CLIの利用可否は`assessment_configuration.active: false`で決まると明記した。旧registryのhashを変更せず、既存のinactive profile拒否とactive Web assessmentを確認する。

## #30 非Web参加レビューの記録・報告・再確認

Web監査runと独立した`non-web-review`を追加した。`init`は文書／スライド、動画／音声、イベント／コミュニティ、参加導線の4種類から、見つける・受け取る・理解する・参加する・続けるの5観点を未実施で生成する。JSON schemaと追加検証で、観点・check IDの欠落や重複、根拠のない確認済み結果、確認者・観測・改善案・再確認方法の不足を拒否する。`report`は人手記録を非公開Markdownへ、`compare`は同じ対象IDの前後版をJSONまたはMarkdownへ新規出力する。比較は申告された内容だけを対象とし、参照資料の実バイトや改善の成立は証明しない。WCAG／JISのoutcome・claimへ自動転用しない。

4種類の初期例、記入済みイベントのreport／compare、誤ったclaimと証拠不足を専用3テストで確認した。README日英11テスト、関連CLI／locale18テスト、release packaging3テストも成功。README日英の対応表を更新し、専用の規格profile・human review queue・証拠ファイルの実体照合は未対応と明示する。出力は私的フォルダに保管する。GitHub Issueはopen、未push・未PR・未merge。

以下の未完了Issueも継続目標に含む。技術的な前提や外部判断が必要な項目は、具体的な残課題を記録して実行可能な作業を進める。

同じ行でも、前提となるIssueから順に実装する。P1は証拠・権限・日時の信頼性、P2は人手作業と再検査、P3は対象拡張と保守性を優先する区分であり、指摘の重大度とは異なる。

| 優先度 | Issue | 前提と残課題 |
| --- | --- | --- |
| P1 | #39 | ローカル実装・検証済み。時差付き入力、実時刻比較、エラー例、旧記録の互換性方針を整備。未push・未merge。 |
| P1 | #25 → #54 → #65 | 3件ともローカル実装・検証済み。実Chromeで証拠付き取り込みからreportまで成功。追加したリモートCIの実行と統合は残る。 |
| P1 | #13 → #63 → #64 | 3件ともローカル実装・検証済み。実Chromeの能力確認・通信制御・承認付きフォーカス確認を接続。未対応操作と支援技術実機は未確認のまま保持する。 |
| P1 | #57 → #58 | 2件ともローカル実装・検証済み。人手レビューの帰属と最終bundle／前段署名の検証を接続。本番の署名者と信頼方針は受領者が別途決定する。長期タイムスタンプと完全な対象保存は未保証。 |
| P1 | #50 → #35 | #50はローカル実装・検証済み。#35も保守文書・フォーム・配布候補と導入dry-runまで完了し、非公開窓口・About設定の確認回答、push／Release公開が残る。候補を公開済みとは扱わない。 |
| P2 | #16 → #17、#31 | 3件ともローカル実装・検証済み。候補作成、人手入力、登録前検証から単独CLIでの統合・報告まで接続済み。 |
| P2 | #42 → #55 → #43 | 3件ともローカル実装・検証済み。観測・指摘の多対多対応と、複数確認者の一致・不一致・訂正履歴を接続済み。 |
| P2 | #41 → #60 | 2件ともローカル実装・検証済み。run補足情報と、規格判定から分離した同意付き当事者テストを接続済み。 |
| P2 | #48 → #49 → #59 | 3件ともローカル実装・検証済み。外部変更、再検査比較、指摘の状態・期限・例外を接続済み。 |
| P2 | #53 → #33 | 両件ともローカル実装・対象検証済み。#53はAI handoffと実行runtime、#33は監査専用の既定導入と任意修正機能を分離。ソースpackageの物理分割とリモートCIは未実施。全体テストは`exceljs`欠落による環境上の未確認が残る。 |
| P2 | #28、#26、#27 | 3件ともローカル実装・対象検証済み。#28は旧run再レポート、#26は内部出力先、#27は版固定と3 OS向け導入・更新・削除を整備。macOS／Linux実機とリモートCI、公開releaseは未確認。 |
| P3 | #14 → #32 | #14は15/55（WCAG）まで段階拡充し、残りは未整備。#32は一覧・任意拡張・主要3パターンをローカル実装し、対象テストが成功。 |
| P3 | #30、#66 | #66はATAGを参照ガイダンスへ統一し、inactive profileとWeb評価を分離。#30は非Webの5観点記録・報告・前後比較をローカル実装。規格profileや根拠ファイル検証は未対応。 |
| P3 | #51 → #46 | 両件ともローカル実装・対象検証済み。#51は共通正本を一本化し、導入先とrelease内の両配布物を生成。#46は週次の公式資料監視と差分候補を実装。リモートCIは未実行。 |

## #51・#46の進捗（2026-09-25）

#51では`shared/skill`を共通本文の追跡対象とし、Codex固有の`agents/openai.yaml`を`platform/codex/agents`へ分離した。既存の`codex/skills/...`と`claude/skills/...`はGitの追跡対象から外して同期生成し、従来の導入先pathは維持する。sourceだけのcloneから両配布物を生成でき、installerと検証器は不足分を生成する。release生成器はcommitに固定したsourceから一時領域で両配布物を生成し、実バイトのhashと展開後のpackageを確認する。CIは生成後にE2Eを実行する。247の共通ファイルを重複追跡せずに済む。

同期器の生成・rollbackテスト28件（2件スキップ）、Codex／Claude導入テスト22件、releaseテスト3件、移行で変わったCLI／registryテスト22件が成功。package・catalog・同期checkも成功した。全体テストは`exceljs`欠落と実ブラウザー能力不足のため対象外を分けて確認した。Ubuntu／WindowsのリモートCI、実ブラウザーE2E、release公開は未実行であり、GitHub上のIssueもopenのまま維持する。

#46ではW3CのWCAG 2.2本体・errata・Understanding・Techniques・ACT・ARIA 1.2、WAICのJISチェックリスト・解説、デジタル庁の方針ページの9件を固定し、週次または手動でURL・転送先・HTTP情報・本文hashを監視する。変更・取得失敗はレビュー対象としてCIを失敗させ、成果物に保持する。既存のcatalog候補生成と差分比較を接続し、正本や`last_verified_at`は自動更新しない。ローカル実行では9件を取得でき、初期baselineとの差は0件。catalog候補の構造差分は0件だが、デジタル庁ページの本文hashだけが保存済みcatalogから変化しており、人手確認が必要。リモートのschedule・artifact uploadは未実行。baseline更新と正本への採用は出典・内容の確認後に別commitで行う。

## #14のfocus手順追加（2026-09-25）

SC 2.4.3（focus順序）と2.4.7（focusの可視性）を英日で追加した。一次資料、動的状態、キーボード操作と人手観測、判定不能条件、別条項との境界を明記。カバレッジはWCAG 14/55（25.5%）、JIS 11/38（28.9%）、旧複合profile 11/56（19.6%）。条項手順の対象テスト、catalog・出典・配布検証が成功。残る41件のWCAG手順は未整備で、#14はopenのまま扱う。

続いてSC 2.1.2（キーボードトラップなし）を英日で追加した。埋め込み部品とdialogの出入り、標準以外の退出操作の案内、2.1.1／2.4.3との境界を明記。利用可能な手順はWCAG 15/55（27.3%）、JIS 12/38（31.6%）、旧複合profile 12/56（21.4%）。手順・日本語表示・CLI探索の対象テストが成功し、残る40件のWCAG手順は未整備。#14はopenのまま維持する。

## 親Issueの修正方針

#144に残っている#7、#9、#12、#18、#19、#20、#21、#22、#23、#29、#45、#61はGitHub上ですでにcloseされている。旧チェックリストを現在の未完了一覧として扱わない。個別Issueの仕様は維持し、親Issueの件数・順序・実装状況を更新する。

## 実装・統合の扱い

- schema、runtime、CLI、reportまで使える状態を一つの実装単位とする。未接続のhelperや未統合PRを解決の根拠に数えない。
- 個別Issueは受け入れ条件の正本として維持し、部分実装ではcloseしない。
- 通常の実装commitを残し、Codex／Claudeの同期と関連する回帰テストを確認する。CIでソースを書き換えるworkflowは追加しない。
- ローカル検証と、Ubuntu／WindowsのCI・push・merge・公開を区別して記録する。
