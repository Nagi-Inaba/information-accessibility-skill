# `scan-web` code-review status

The implementation remains unmerged until the normal verification matrix, real Chromium E2E, and a final independent code-review pass all succeed on the same head commit.
